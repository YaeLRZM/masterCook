# --- Web framework ---------------------------------------------------------
fastapi==0.129.0
uvicorn==0.40.0
python-multipart==0.0.27        # requerido por UploadFile (imagenes)

# --- Validacion / esquemas -------------------------------------------------
pydantic==2.12.5
email-validator==2.3.0          # requerido por pydantic.EmailStr

# --- ORM / Base de datos ---------------------------------------------------
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11         # driver PostgreSQL
alembic==1.18.4                 # migraciones

# --- Seguridad / JWT -------------------------------------------------------
python-jose==3.5.0              # firma/verificacion del JWT
passlib==1.7.4                  # hashing PBKDF2 de passwords

# --- Configuracion ---------------------------------------------------------
python-dotenv==1.2.1            # carga del archivo .env
